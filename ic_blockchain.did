type Block = record {
  certificate: blob;
  data: vec blob;
  previous_hash: blob;
};

service blockchain: {
  append: (data: vec blob) -> (nat);
  append_unique: (data: vec blob) -> (opt nat);
  get_block: (index: nat) -> Block;
  find_data: (hash: blob) -> (opt nat);
  last: () -> (nat);
}
